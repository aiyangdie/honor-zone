# 数据库配置
DB_USER=game_user
DB_PASSWORD=game_password
DB_HOST=localhost
DB_NAME=game_leaderboard

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Flask配置
FLASK_DEBUG=True
FLASK_PORT=5000

# 同步服务配置
SYNC_INTERVAL=300  # 同步间隔(秒)，默认5分钟